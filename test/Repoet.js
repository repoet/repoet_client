const MyStringStore = artifacts.require("./Repoet.sol")

contract("Repoet", accounts => {
  it("should save new poems", async () => {
    
  });

  it("should reject POEM content that is taken", async () => {
    
  });

  it("should enable transfer of POEM tokens", async () => {
    
  });
});